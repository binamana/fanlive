diff --git a/lib/main.dart b/lib/main.dart
index df2a3b5..3a31f94 100644
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -4,6 +4,9 @@ import 'dart:async';
 import 'models/broadcast_record.dart';
 import 'package:shared_preferences/shared_preferences.dart';
 import 'dart:convert';
+import 'widgets/fan_input.dart';
+import 'widgets/fanlive_background.dart';
+import 'widgets/glass_card.dart';
 
 void main() async {
   WidgetsFlutterBinding.ensureInitialized();
@@ -491,90 +494,6 @@ SizedBox(
   }
 }
 
-class FanInput extends StatelessWidget {
-  final TextEditingController controller;
-  final String label;
-  final String hint;
-
-  const FanInput({
-    super.key,
-    required this.controller,
-    required this.label,
-    required this.hint,
-  });
-
-  @override
-  Widget build(BuildContext context) {
-    return TextField(
-      controller: controller,
-      style: const TextStyle(color: Colors.white),
-      decoration: InputDecoration(
-        labelText: label,
-        hintText: hint,
-        labelStyle: const TextStyle(color: Colors.white70),
-        hintStyle: const TextStyle(color: Colors.white30),
-        filled: true,
-        fillColor: Colors.white.withOpacity(0.08),
-        border: OutlineInputBorder(
-          borderRadius: BorderRadius.circular(18),
-          borderSide: BorderSide(color: Colors.white.withOpacity(0.2)),
-        ),
-        enabledBorder: OutlineInputBorder(
-          borderRadius: BorderRadius.circular(18),
-          borderSide: BorderSide(color: Colors.white.withOpacity(0.15)),
-        ),
-      ),
-    );
-  }
-}
-
-class FanLiveBackground extends StatelessWidget {
-  final Widget child;
-
-  const FanLiveBackground({super.key, required this.child});
-
-  @override
-  Widget build(BuildContext context) {
-    return Scaffold(
-      body: Container(
-        width: double.infinity,
-        decoration: const BoxDecoration(
-          gradient: LinearGradient(
-            colors: [
-              Color(0xFF120018),
-              Color(0xFF2B0B3F),
-              Color(0xFF151022),
-            ],
-            begin: Alignment.topLeft,
-            end: Alignment.bottomRight,
-          ),
-        ),
-        child: child,
-      ),
-    );
-  }
-}
-
-class GlassCard extends StatelessWidget {
-  final Widget child;
-
-  const GlassCard({super.key, required this.child});
-
-  @override
-  Widget build(BuildContext context) {
-    return Container(
-      width: double.infinity,
-      padding: const EdgeInsets.all(20),
-      decoration: BoxDecoration(
-        color: Colors.white.withOpacity(0.08),
-        borderRadius: BorderRadius.circular(24),
-        border: Border.all(color: Colors.white.withOpacity(0.15)),
-      ),
-      child: child,
-    );
-  }
-}
-
 ButtonStyle fanButtonStyle() {
   return ElevatedButton.styleFrom(
     backgroundColor: const Color(0xFFFF4FB8),
@@ -1708,4 +1627,4 @@ GlassCard(
       ),
     );
   }
-}
\ No newline at end of file
+}
diff --git a/lib/widgets/glass_card.dart b/lib/widgets/glass_card.dart
new file mode 100644
index 0000000..8a8f33f
--- /dev/null
+++ b/lib/widgets/glass_card.dart
@@ -0,0 +1,21 @@
+import 'package:flutter/material.dart';
+
+class GlassCard extends StatelessWidget {
+  final Widget child;
+
+  const GlassCard({super.key, required this.child});
+
+  @override
+  Widget build(BuildContext context) {
+    return Container(
+      width: double.infinity,
+      padding: const EdgeInsets.all(20),
+      decoration: BoxDecoration(
+        color: Colors.white.withOpacity(0.08),
+        borderRadius: BorderRadius.circular(24),
+        border: Border.all(color: Colors.white.withOpacity(0.15)),
+      ),
+      child: child,
+    );
+  }
+}
diff --git a/lib/widgets/fanlive_background.dart b/lib/widgets/fanlive_background.dart
new file mode 100644
index 0000000..7c937e8
--- /dev/null
+++ b/lib/widgets/fanlive_background.dart
@@ -0,0 +1,28 @@
+import 'package:flutter/material.dart';
+
+class FanLiveBackground extends StatelessWidget {
+  final Widget child;
+
+  const FanLiveBackground({super.key, required this.child});
+
+  @override
+  Widget build(BuildContext context) {
+    return Scaffold(
+      body: Container(
+        width: double.infinity,
+        decoration: const BoxDecoration(
+          gradient: LinearGradient(
+            colors: [
+              Color(0xFF120018),
+              Color(0xFF2B0B3F),
+              Color(0xFF151022),
+            ],
+            begin: Alignment.topLeft,
+            end: Alignment.bottomRight,
+          ),
+        ),
+        child: child,
+      ),
+    );
+  }
+}
diff --git a/lib/widgets/fan_input.dart b/lib/widgets/fan_input.dart
new file mode 100644
index 0000000..ef4331b
--- /dev/null
+++ b/lib/widgets/fan_input.dart
@@ -0,0 +1,38 @@
+import 'package:flutter/material.dart';
+
+class FanInput extends StatelessWidget {
+  final TextEditingController controller;
+  final String label;
+  final String hint;
+
+  const FanInput({
+    super.key,
+    required this.controller,
+    required this.label,
+    required this.hint,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    return TextField(
+      controller: controller,
+      style: const TextStyle(color: Colors.white),
+      decoration: InputDecoration(
+        labelText: label,
+        hintText: hint,
+        labelStyle: const TextStyle(color: Colors.white70),
+        hintStyle: const TextStyle(color: Colors.white30),
+        filled: true,
+        fillColor: Colors.white.withOpacity(0.08),
+        border: OutlineInputBorder(
+          borderRadius: BorderRadius.circular(18),
+          borderSide: BorderSide(color: Colors.white.withOpacity(0.2)),
+        ),
+        enabledBorder: OutlineInputBorder(
+          borderRadius: BorderRadius.circular(18),
+          borderSide: BorderSide(color: Colors.white.withOpacity(0.15)),
+        ),
+      ),
+    );
+  }
+}